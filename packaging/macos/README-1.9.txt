Argonaut 1.9 qualification build — Apple Silicon, macOS 15 or newer

Open the DMG and drag Argonaut into Applications, then launch it there.
The ZIP contains the same app. Do not run directly from the read-only DMG.
Python, GTK, and the media runtime are bundled.

This is an ad-hoc-signed Argonaut build, not Apple Developer ID signed or notarized.
macOS may block its first launch. Only for this trusted download, use the
per-app Open Anyway option in System Settings > Privacy & Security if offered.
Do not disable Gatekeeper globally.

This package is for Stable 1.9 platform qualification. Passing automated checks
does not make it a published release; verify its exact build identity and use the
matching qualification checklist.
